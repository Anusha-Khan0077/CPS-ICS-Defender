# CPS/ICS Defender — Runtime Dependencies
# Install with: pip install -r requirements.txt
#
# Philosophy: minimal, production-grade deps only.
# No PyTorch / TensorFlow — DQN is pure NumPy.
# No gym / gymnasium — custom Gym-like environment.
# No Mininet — MockController works out of the box.

# Core scientific stack
numpy>=1.24,<3.0
pandas>=2.0,<3.0
scikit-learn>=1.3,<2.0
joblib>=1.3          # Used by scikit-learn; explicit pin for model I/O

# Config & serialisation
pyyaml>=6.0,<7.0

# Lightweight REST API (optional — only needed for Ryu SDN controller)
flask>=3.0,<4.0      # Used by scripts that expose a status endpoint

# Dev / test (install with: pip install -r requirements-dev.txt)
# Listed here for completeness; not required at runtime
# pytest>=8.0
# pytest-cov>=5.0
